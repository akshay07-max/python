# Encapsulation provides:
# Data protection: prevents accidental data modification,
# validation: Can validate data before setting it.
# Flexibility: Internal implementation can change without external changes.
# control: you have full access over how the data is accessed and modified.


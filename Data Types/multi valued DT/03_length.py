# // the len(): the default function used to get the length of the string or indices present.
# only supports multi valued data types.

# syntax 
# len(collection)\

#eg:

st = "The name is Khan"

print(len(st))
